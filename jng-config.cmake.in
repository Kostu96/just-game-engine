
@PACKAGE_INIT@

set_and_check(YAML_CPP_INCLUDE_DIR "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@")
set_and_check(YAML_CPP_LIBRARY_DIR "@PACKAGE_CMAKE_INSTALL_LIBDIR@")

# Our library dependencies (contains definitions for IMPORTED targets)
include(@PACKAGE_CONFIG_EXPORT_DIR@/jng-targets.cmake)

# These are IMPORTED targets created by jng-targets.cmake
set(JNG_LIBRARIES "@EXPORT_TARGETS@")

check_required_components(@EXPORT_TARGETS@)
